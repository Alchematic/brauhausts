export type Style = {
  name: string;
  category: string;
  og: [number, number];
  fg: [number, number];
  ibu: [number, number];
  color: [number, number];
  abv: [number, number];
  carb: [number, number];
};
